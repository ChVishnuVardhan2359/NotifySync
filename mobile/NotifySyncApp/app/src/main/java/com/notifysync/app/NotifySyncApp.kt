package com.notifysync.app

import android.app.Application

/**
 * Application entry point. WorkManager self-initializes via its merged manifest provider,
 * so no custom configuration is required here.
 */
class NotifySyncApp : Application()
